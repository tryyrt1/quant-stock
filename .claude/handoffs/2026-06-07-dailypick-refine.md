# 2026-06-07 交班 — 每日一股改进+横盘突破

## 本次改动

### 每日一股
- 新增 `🔄 重新选股` 按钮（清缓存+重新计算+自动轮询）
- 自动扫描改为**手动触发**（09:25 调度器仍在后台自动算）
- 新增 **跳空加分**（集合竞价开盘结果影响排名）
- 新增 **横盘扫描痕迹**（底部显示扫描结果，即使0只）

### 选股模式
- 新增 `横盘启动` 形态（在 patternOrder 中，0只也显示灰色）
- 删除 `长下影+中阳` 形态

### 评分增强
- 7维度 + 周线加分 + 量价加分 + 横盘启动加分 + 跳空加分 + ML评分修正

### 涉及文件
- `server.py` — 跳空加分、刷新API、scan_summary
- `static/index.html` — 重新选股按钮、扫描痕迹、横盘启动

## 已知问题
- 缓存中旧数据无 scan_summary 字段，需重新计算后才显示
- 前端多次 git checkout 恢复导致部分改动丢失，已重新补上

## 未完成
- 远端服务器可能仍有缓存不匹配问题，下次需 `rm data/dailypick.json` 后重算

## 部署
- 云服务器已部署
